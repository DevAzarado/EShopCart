@model IEnumerable<Promotions>


<h4 class="d-flex justify-content-between align-items-center mb-3">
    <span class="text-primary">Promotions</span>
    <span class="badge bg-primary rounded-pill">You Cart</span>
</h4>

<ul class="list-group mb-3">

    @foreach (var item in Model)
    {

       @* <li class="list-group-item d-flex justify-content-between lh-sm">
            <div>
                <h6 class="my-0">Product name</h6>
                <small class="text-muted">Brief description</small>
            </div>
            <span class="text-muted">
                <a class="btn btn-primary btn-sm" asp-controller="Cart" asp-action="Add" asp-route-id=5>Adicionar</a>
            </span>
        </li>*@
        <li class="list-group-item d-flex justify-content-between lh-sm">
            <div>
                <h6 class="my-0">@item.Name</h6>
                <small class="text-muted">@item.Description</small>
            </div>
            <span class="text-muted">
                <a class="" asp-controller="Cart" asp-action="Add" asp-route-id=@item.Id> R$ @item.Price.ToString("C")</a>
            </span>

        </li>

    }
</ul>


